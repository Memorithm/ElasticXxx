[package]
name = "elastic-kernel"
version = "0.1.0"
license-file.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
publish = false
description = "Generic kernel-realization planning contracts for ElasticXxx"

[dependencies]
elastic-core = { package = "memorithm-elastic-core", path = "../elastic-core" }
elastic-eir = { package = "memorithm-elastic-eir", path = "../elastic-eir" }
serde = { workspace = true }
serde_json = "1.0"

[dev-dependencies]
elastic = { package = "memorithm-elastic", path = "../elastic" }

[[bench]]
name = "be14f_kernel_realization"
harness = false
