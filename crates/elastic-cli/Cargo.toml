[package]
name = "elastic-cli"
version = "0.1.0"
edition = "2021"
rust-version = "1.89"

[dependencies]
elastic = { path = "../elastic" }
clap = { version = "4.5", features = ["derive"] }
serde_json = "1.0"
