[package]
name = "elastic-adapters"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
publish = false
description = "Concrete in-process Elastic resource adapters: RAM budgets and concurrency permits"

[dependencies]
elastic-core = { workspace = true }
elastic-eir = { workspace = true }
serde.workspace = true

[dev-dependencies]
serde_json = "1.0"
