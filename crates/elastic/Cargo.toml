[package]
name = "elastic"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
publish = false
description = "User-facing facade for ElasticXxx declarations, runtime, and reference adapters"

[dependencies]
elastic-core = { workspace = true }
elastic-eir = { workspace = true }
elastic-macros = { workspace = true }
elastic-runtime = { workspace = true }
elastic-adapters = { workspace = true }

[dev-dependencies]
trybuild = "1"
